# --- Application Settings ---
# The port where the web interface will be accessible
APP_PORT=8080

# The URL for the service (Required for OAuth/Reverse Proxy/etc)
# Must be a full URL, e.g., https://service.example.com
PUBLIC_URL=https://sub.domain.com

# --- System / Permissions ---
# Run `id $USER` on your host to find these values
PUID=1000
PGID=1000
TZ=America/New_York

# --- Data Volumes ---
# It is recommended to change these to absolute paths on your host
# e.g., /mnt/storage/service-name/config
CONFIG_DIR=./config
DATA_DIR=./data

# --- Media Volumes (If Applicable) ---
MEDIA_DIR=./media
DOWNLOADS_DIR=./downloads
