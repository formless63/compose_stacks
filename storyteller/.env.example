# --- Application Settings ---
APP_PORT=8001
# Required for OAuth. Must be a full URL (e.g. https://sub.domain.com/api/v2/auth) - note that /api/v2/auth is required.
AUTH_URL=https://my.website.com/api/v2/auth

# --- Security (CRITICAL) ---
# Generate with: openssl rand -base64 32
STORYTELLER_SECRET_KEY=

# --- Feature Flags ---
ENABLE_WEB_READER=false

# --- Data Volumes ---
DATA_DIR=./data
